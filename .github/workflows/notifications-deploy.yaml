name: deploy notifications worker

on:
  workflow_dispatch:
    inputs:
      stage:
        description: The Alchemy stage to operate on
        required: true
        default: preview
        type: choice
        options:
          - preview
          - production
      dry_run:
        description: Plan without creating a deployment
        required: false
        default: false
        type: boolean
  pull_request:
    branches:
      - main
    types: [opened, reopened, synchronize, closed]
    paths:
      - "apps/notifications/**"
      - ".github/workflows/notifications-deploy.yaml"
      - ".github/workflows/reusable-deploy.yaml"
      - "packages/**"
      - "pnpm-workspace.yaml"
      - "package.json"
      - "pnpm-lock.yaml"
      - "turbo.json"
  push:
    branches:
      - main
    paths:
      - "apps/notifications/**"
      - ".github/workflows/notifications-deploy.yaml"
      - ".github/workflows/reusable-deploy.yaml"
      - "packages/**"
      - "pnpm-workspace.yaml"
      - "package.json"
      - "pnpm-lock.yaml"
      - "turbo.json"

permissions:
  contents: read

jobs:
  deploy:
    if: github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository
    uses: ./.github/workflows/reusable-deploy.yaml
    secrets:
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
      NOTIFICATIONS_GITHUB_TOKEN: ${{ secrets.NOTIFICATIONS_GITHUB_TOKEN }}
      NOTIFICATIONS_REPO_TOKEN: ${{ secrets.NOTIFICATIONS_REPO_TOKEN }}
    with:
      app: notifications
      stage: ${{ github.event_name == 'pull_request' && format('pr-{0}', github.event.number) || (github.event_name == 'push' && 'production' || inputs.stage) }}
      operation: ${{ github.event_name == 'pull_request' && github.event.action == 'closed' && 'destroy' || (github.event_name == 'workflow_dispatch' && inputs.dry_run && 'plan' || 'deploy') }}
      build_arg: build:notifications
      typecheck_arg: turbo run typecheck --filter=notifications
      test_arg: turbo run test --filter=notifications
